# =========================================================================
# NEVEN: MOTOR DE EXTRACCIÓN DE OUTPUTS
# =========================================================================
#
# NOTA: Las funciones Extraer_outputs(), .neven_procesar_valor() y
# .neven_consolidar() están definidas canónicamente en startup/startup.r
# (se cargan automáticamente al inicio de cada sesión R).
#
# Este archivo se mantiene vacío para evitar duplicación.
# Los procedimientos de la librería (R4XCL-RG-*.R) invocan Extraer_outputs()
# que ya está disponible en el entorno global via startup.r.
# =========================================================================
